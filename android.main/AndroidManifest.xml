<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sneer.android.main"
    android:versionCode="2"
    android:versionName="1.0"
    android:installLocation="internalOnly">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <receiver android:name=".CloudBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.InteractionListActivity"
            android:label="@string/title_interaction_list" >
        </activity>
        
        <activity
            android:name=".ui.InteractionActivity"
            android:label="@string/title_interaction" >
        </activity>

        <activity
            android:name=".ui.ProfileActivity"
            android:label="@string/title_activity_profile" />

        <service
            android:exported="true"
            android:name=".CloudService" >
            <intent-filter>
                <action android:name="sneer.intent.action.BIND_CLOUD_SERVICE" />
            </intent-filter>
         </service>
    </application>

</manifest>
