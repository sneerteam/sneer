ext {
    external = [
        'rx': [version: '0.20.3'],
        'clojure.core.match': 'org.clojure:core.match:0.2.2'
    ]
}

buildscript {
    repositories {
        mavenCentral()
    }
}

allprojects {
    group = 'me.sneer'
    repositories {
        mavenCentral()
    }
}
